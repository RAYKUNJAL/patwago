{
  "name": "patwago",
  "private": true,
  "version": "1.1.0",
  "description": "PatWaGo: Jamaican travel companion with vendors, trips, photos, guardian mode, and local backend.",
  "type": "commonjs",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/app-pages.js && node --check lib/ai-agent.js && node --check lib/translator.js && node --check lib/analytics.js && node --check public/app/analytics.js && node --check public/app/admin.js && node --check public/app/app.js && node --check tests/api.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "launchConfig": {
    "ai": "xAI Grok with catalog-grounded fallback",
    "stt": "Speaches/faster-whisper",
    "tts": "xAI expressive/custom Jamaican voice; Chatterbox fallback"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
